name: menu_item_widget
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^4.3.3

  cupertino_icons: ^1.0.1

  commonwidget:
    git:
      url: https://git-codecommit.eu-west-1.amazonaws.com/v1/repos/flutter_widgets
      ref: master
      path: common_widget


dev_dependencies:
  flutter_test:
    sdk: flutter

  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true